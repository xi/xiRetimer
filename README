xiRetimer v 0.1

breaks an audio file into pices, timestretches and reconcatenates them. 
I want to use this to fit recorded Audio exactly to a tempo.

playback - gstreamer
gui - wxwidgets
xiRetimer
timestretch - rubberband


TODO

draw to different buffers only when needed
getRatio(float) -> interpolation
exits with error
bugtracking
im/export different kinds of audio files (libmad)
soundtouch
